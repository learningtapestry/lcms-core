<% url = @resource.persisted? ? admin_resource_path(@resource) : admin_resources_path %>
<%= simple_form_for @resource, url: url do |f| %>
  <%= render partial: 'fields', locals: { f: f, resource: @resource } %>
<% end %>
